name: CI Pipeline TecnoRepuestos

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Validar sintaxis y pruebas
      run: |
        echo "Ejecutando validaciones iniciales..."
        # Aquí se ejecutarán los tests automáticos a futuro